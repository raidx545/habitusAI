[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "interior-ai"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.py-modules]
# must be array — list root-level .py modules here
